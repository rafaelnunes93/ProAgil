/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTipeGeneratePipePipe } from './DateTipeGeneratePipe.pipe';

describe('Pipe: DateTipeGeneratePipee', () => {
  it('create an instance', () => {
    let pipe = new DateTipeGeneratePipePipe();
    expect(pipe).toBeTruthy();
  });
});
